apiVersion: v1
kind: Service
metadata:
  name: techno-ml-service
  namespace: default
  labels:
    app: techno-ml-app
spec:
  type: LoadBalancer
  selector:
    app: techno-ml-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 2000